1、系统将不在处理AGV停止状态。
即仅当AGV未完成任务且处于待机状态时，系统才会重新分配任务至AGV。
若AGV处于停止状态，则需要AGV手动复位至待机状态，系统才会重新分配任务。
2、在处理关键任务时,如上料下料等任务，AGV应保证自身一定处于地标卡的正上方(即地标卡处于RFID的扫描范围)才可以执行此任务。
若AGV不满足条件，则应将当前地标卡置0并上报错误。等待系统重新分配任务至AGV。


1、为了解决有可能因传递消息结构体指针引起的内存泄露问题，程序在运行时生成全局消息结构体，并在结构体更新时，通知界面更新相关内容。
2、为了解决程序关闭时，因消息队列引起的读取野指针的行为，程序需实现消息线程用于接收后台传至前台的消息。
3、为了能够监测交通管制机制是否正常运行，为程序增加交通管制监测机制。

需要实现的功能：
1、地图的导入功能
2、系统日志功能
3、关键信息的完全监控，包括Debug以及Release版本
4、由于数据库写入读取等操作造成的主线程延时，将要执行的SQL语句，存入列表中，并通过互斥锁保证线程的安全性。